{
  "name": "claude-context-render",
  "version": "1.0.0",
  "description": "A tool that collects CLAUDE.md files from directory hierarchy (project folder up to ~/.claude/), resolves/embeds their @-imports, and generates processed context files can then be used as context for Gemini.",
  "main": "index.js",
  "bin": {
    "claude-context-render": "./index.js"
  },
  "scripts": {
    "build": "node index.js",
    "test": "node index.js"
  },
  "keywords": [
    "cli",
    "claude",
    "context"
  ],
  "author": "Carlo Zottmann <carlo@zottmann.dev>",
  "license": "MIT",
  "dependencies": {
    "commander": "^14.0.0"
  }
}
