- [x] add support for CLI commands and flags using npm package [Commander](https://www.npmjs.com/package/commander)
- [x] the main tool of the package the user will call is named "claude-context-render"
- [x] add `create` command (default) for creating output files
- [x] add `setup` command for adding the output file name to Gemini settings' `contextFileName` array
    - the Gemini settings file is `~/.gemini/settings.json`, see https://raw.githubusercontent.com/google-gemini/gemini-cli/refs/heads/main/docs/cli/configuration.md for a structure description
- [x] add `cleanup` command for removing output files
- [x] add `--output-folder` flag for  specifying where to place the output file
    - `global`: one collated file in `~/.gemini/`
    - `project`: one collated file in the current folder
    - `origin`: one single file next to its originating `CLAUDE.md` file
- [x] add `--filename` flag for defining the output file name (default: "CLAUDE-derived.md")
